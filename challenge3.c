#include <stdio.h>

int main() {
  
  float km,y;
     
     printf("entre la distance en kilomètres ");
     scanf("%f",&km);
    y=km*1093.61;
     printf("la distance en yards = %.2f",y);
     
  
    return 0;
}